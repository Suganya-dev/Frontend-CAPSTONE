import React from "react"
import "./Activity.css"

export const ActivityCard = ({Activity}) => {
  return <section className = "activities">
      <h2 className="activities__name">{Activity.name}</h2>
  </section>

}




















